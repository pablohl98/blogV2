<div id="<%= dom_id category %>">
  <p>
    <%= category.name %>
    <%= link_to 'Editar Categoría', edit_category_path(category) %>
    <%= link_to 'Eliminar Categoría', category_path(category), data: { turbo_method: :delete, turbo_confirm: 'Estas seguro?' } %> 
  </p>

</div>
