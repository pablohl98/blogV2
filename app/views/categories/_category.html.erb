
<tr id="<%= dom_id category %>">
  <td><%= category.id %></td>
  <td><%= category.name %></td>
  <td>
    <%= link_to 'Editar Categoría - ', edit_category_path(category) %>
    <%= link_to 'Eliminar Categoría', category_path(category), data: { turbo_method: :delete, turbo_confirm: '¿Estás seguro?' } %> 
  </td>
</tr>

